{
  "name": "nullid",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "typecheck": "tsc -b",
    "lint": "node scripts/lint.js",
    "test": "tsc -p tsconfig.test.json && node --test build-test/__tests__/*.js",
    "e2e": "playwright test",
    "validate": "npm run typecheck && npm run lint && npm run test && npm run e2e && npm run build"
  },
  "dependencies": {
    "@noble/hashes": "^1.4.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "devDependencies": {
    "@playwright/test": "^1.48.2",
    "@types/react": "^18.3.10",
    "@types/react-dom": "^18.3.0",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.3",
    "vite": "^5.4.10"
  }
}
